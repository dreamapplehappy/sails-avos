	<h1>ad</h1>
	<script type="text/javascript">
    var user = new AV.User();
    user.set("username", "my name");
    user.set("password", "my pass");
    user.set("email", "email@example.com");
    user.set("phone", "415-392-0202");

    user.signUp(null, {
      success: function(user) {
        alert("user create success!");
      },
      error: function(user, error) {
        alert("Error: " + error.code + " " + error.message);
      }
    });
	</script>

  <% if(AV.User.current()){ %>
                <li><a href="#">username</a></li>
                <li><a href="/user/logout">logout</a></li>
        <% }else{ %>
                <li><a href="/user/login">login</a></li>
        <% } %>


        <% if(user){ %>
                <li><a href="#"><%= user.username%></a></li>
                <li><a href="/user/logout">logout</a></li>
        <% }else{ %>
                <li><a href="/user/login">login</a></li>
        <% } %>



        <% if(currentUser){ %>
                <li><a href="#"><%= currentUser.username%></a></li>
                <li><a href="/user/logout">logout</a></li>
        <% }else{ %>
                <li><a href="/user/login">login</a></li>
        <% } %>





        <form class="form-inline">
          <div class="form-group">
            <label for="username">用户名：</label>
            <input type="text" class="form-control" id="username" placeholder="dreamapple">
        </div>
        <div class="form-group">
            <label for="email">Email：</label>
            <input type="email" class="form-control" id="email" placeholder="dreamapple@163.com">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-warning">重置</button>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary">确认添加</button>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-danger">删除</button>
        </div>
    </form>
